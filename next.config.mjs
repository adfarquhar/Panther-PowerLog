/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   ppr: true, // If you want to try out Partial Prerendering (experimental in Next.js 14, may change)
  // },
  // If you add any local packages that need transpiling later, add them here:
  // transpilePackages: ['@your/local-package'],
};

export default nextConfig; 